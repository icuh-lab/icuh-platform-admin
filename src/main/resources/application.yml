server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://icuh-rds.cd7bwwfid5u3.ap-northeast-2.rds.amazonaws.com:3306/ACTUAL_DRGHT?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dasom
    password: dasom123

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  config:
    import: application-private.yml
